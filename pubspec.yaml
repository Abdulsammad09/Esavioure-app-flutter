name: esavier
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: any
  audioplayers: ^6.1.0
  url_launcher: ^6.3.0
  shared_preferences: ^2.3.2







dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # Ensure correct indentation for assets
  assets:
    - assets/images/
    - assets/audio/a.mp3
    - assets/audio/b.mp3
